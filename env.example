# Arduino WiFi Configuration
# Configure your Arduino Uno R4 WiFi connection

# Arduino IP Address (find this in your Arduino Serial Monitor or WiFi settings)
# Example: 192.168.1.100, 192.168.0.50, etc.
ARDUINO_IP=192.168.1.100

# Arduino Web Server Port (default is 80)
ARDUINO_PORT=80

# Arduino Command Endpoint (default is /command)
ARDUINO_ENDPOINT=/command

# Server Port (optional, defaults to 3000)
PORT=3000

# Arduino CLI Configuration (for code uploads)
# Path to Arduino CLI executable (optional, defaults to 'arduino-cli')
ARDUINO_CLI_PATH=arduino-cli

# Arduino board type (optional, defaults to Uno R4 WiFi)
ARDUINO_BOARD=arduino:renesas_uno:unor4wifi

# Arduino upload port for code uploads (optional, leave empty for compilation only)
# Examples: /dev/ttyUSB0 (Linux), COM3 (Windows), /dev/tty.usbmodem* (macOS)
ARDUINO_UPLOAD_PORT=/dev/ttyUSB0 